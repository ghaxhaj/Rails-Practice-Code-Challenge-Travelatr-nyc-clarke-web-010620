<h1> Post </h1>

Title: <%= @post.title %>
<p><%= @post.content %></p>
<p>Likes: <%= @post.likes %></p>

<h2> Blogger </h2>
<p><%= link_to @blogger.name, blogger_path(@blogger) %></p>

<h2>Destination</h2>
<p><%= link_to @destination.name, destination_path(@destination) %></p>

<%= button_to "Like post", like_path(@post.id), method: :post %>
<%= button_to "Unlike post", unlike_path(@post.id), method: :post %>
<%= button_to "All posts", posts_path, method: :get %>
<%= button_to "Edit post", edit_post_path, method: :get %>
<%= button_to "Delete", post_path, method: :delete %>